<Window x:Class="CarRental.Desktop.WPF.ClientManagementWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Gestion des Clients (WPF)" Height="650" Width="900">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto">
            <Grid Margin="0,0,0,15">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Grid.Column="0" Margin="5">
                    <Label Content="Prénom (FirstName) :"/>
                    <TextBox x:Name="txtFirstName" Height="30"/>
                </StackPanel>
                <StackPanel Grid.Row="1" Grid.Column="0" Margin="5">
                    <Label Content="Nom (LastName) :"/>
                    <TextBox x:Name="txtLastName" Height="30"/>
                </StackPanel>
                <StackPanel Grid.Row="2" Grid.Column="0" Margin="5">
                    <Label Content="E-mail :"/>
                    <TextBox x:Name="txtEmail" Height="30"/>
                </StackPanel>
                <StackPanel Grid.Row="3" Grid.Column="0" Margin="5">
                    <Label Content="Téléphone (Phone) :"/>
                    <TextBox x:Name="txtPhone" Height="30"/>
                </StackPanel>

                <StackPanel Grid.Row="0" Grid.Column="1" Margin="5">
                    <Label Content="Date de Naissance :"/>
                    <DatePicker x:Name="dpBirthDate" Height="30"/>
                </StackPanel>
                <StackPanel Grid.Row="1" Grid.Column="1" Margin="5">
                    <Label Content="Permis de Conduire (License) :"/>
                    <TextBox x:Name="txtDriverLicenseNumber" Height="30"/>
                </StackPanel>
                <StackPanel Grid.Row="2" Grid.Column="1" Grid.RowSpan="2" Margin="5">
                    <Label Content="Adresse (Address) :"/>
                    <TextBox x:Name="txtAddress" Height="60" TextWrapping="Wrap" AcceptsReturn="True"/>
                </StackPanel>

            </Grid>
        </ScrollViewer>

        <DataGrid x:Name="dgvClients" 
                  Grid.Row="1" 
                  AutoGenerateColumns="True" 
                  IsReadOnly="True" 
                  SelectionMode="Single"
                  Margin="0,0,0,15"
                  SelectionChanged="DgvClients_SelectionChanged"
                  AutoGeneratingColumn="DgvClients_AutoGeneratingColumn"/>
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center">
            <Button x:Name="btnAdd" Content="Ajouter" Width="100" Margin="10,0" Click="BtnAdd_Click"/>
            <Button x:Name="btnUpdate" Content="Modifier" Width="100" Margin="10,0" Click="BtnUpdate_Click"/>
            <Button x:Name="btnDelete" Content="Supprimer" Width="100" Margin="10,0" Click="BtnDelete_Click"/>
            <Button x:Name="btnClear" Content="Effacer" Width="100" Margin="10,0" Click="BtnClear_Click"/>
        </StackPanel>
    </Grid>
</Window>