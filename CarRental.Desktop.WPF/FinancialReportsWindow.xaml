<Window x:Class="CarRental.Desktop.WPF.FinancialReportsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:CarRental.Desktop.WPF.ViewModels"
        mc:Ignorable="d"
        Title="Rapports Financiers" Height="600" Width="800"
        FontFamily="Segoe UI"
        d:DataContext="{d:DesignInstance vm:FinancialReportViewModel}">

    <Window.Resources>
        <Style x:Key="ActionButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#007ACC"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#005C99"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Vertical" Margin="0,0,0,20">
            <TextBlock Text="Rapport de Revenu Mensuel"
                       FontSize="24"
                       FontWeight="Bold"
                       Foreground="#007ACC"
                       Margin="0,0,0,10"/>

            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Du :" VerticalAlignment="Center" Margin="0,0,5,0"/>
                <DatePicker SelectedDate="{Binding StartDate}" Width="120" />

                <TextBlock Text="Au :" VerticalAlignment="Center" Margin="15,0,5,0"/>
                <DatePicker SelectedDate="{Binding EndDate}" Width="120" />

                <Button Content="Actualiser"
                         Margin="15,0,0,0"
                         Padding="10,5"
                         Background="#4CAF50"
                         Foreground="White"
                         Command="{Binding LoadSummaryCommand}"/>
            </StackPanel>
        </StackPanel>

        <Grid Grid.Row="1" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="#E3F2FD" BorderBrush="#007ACC" BorderThickness="1" Padding="15" Margin="0,0,15,0" CornerRadius="8">
                <StackPanel>
                    <TextBlock Text="Nombre de Réservations" FontSize="12" Foreground="#007ACC"/>
                    <TextBlock Text="{Binding Summary.TotalReservationsCount}"
                               FontWeight="Bold" FontSize="28" Foreground="#007ACC"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="1" Background="#FFF3E0" BorderBrush="#FFC107" BorderThickness="1" Padding="15" Margin="0,0,15,0" CornerRadius="8">
                <StackPanel>
                    <TextBlock Text="Total Montant Dû (Réservations)" FontSize="12" Foreground="#FFC107"/>
                    <TextBlock Text="{Binding Summary.TotalReservationRevenue, StringFormat=C0}"
                               FontWeight="Bold" FontSize="28" Foreground="#FFC107"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="2" Background="#E8F5E9" BorderBrush="#4CAF50" BorderThickness="1" Padding="15" CornerRadius="8">
                <StackPanel>
                    <TextBlock Text="REVENU TOTAL (Paiements Reçus)" FontSize="12" Foreground="#4CAF50"/>
                    <TextBlock Text="{Binding Summary.TotalPaymentsReceived, StringFormat=C0}"
                               FontWeight="Bold" FontSize="28" Foreground="#4CAF50"/>
                </StackPanel>
            </Border>
        </Grid>

        <TextBlock Grid.Row="2"
                   Text="Détails du Rapport (À implémenter : Liste des paiements/réservations)"
                   Foreground="#999"
                   VerticalAlignment="Top"
                   Margin="0,10,0,0"/>

        <Button Grid.Row="3"
                Content="Exporter en PDF (Prochaine Étape)"
                Height="40"
                Margin="0,20,0,0"
                Style="{StaticResource ActionButtonStyle}"
                HorizontalAlignment="Right"
                Command="{Binding GeneratePdfCommand}"/>

    </Grid>
</Window>