<Window x:Class="CarRental.Desktop.WPF.VehicleTypeManagementWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Gestion des Types de Véhicules (WPF)" Height="450" Width="700">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,15">

            <Grid Margin="0,0,0,5">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="150"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Label Grid.Column="0" Content="Nom du Type de Véhicule :" VerticalAlignment="Center"/>
                <TextBox x:Name="txtVehicleTypeName" 
                         Grid.Column="1" 
                         Height="30" 
                         Margin="5"
                         Text="{Binding SelectedVehicleType.Name, UpdateSourceTrigger=PropertyChanged}"/>
            </Grid>

            <Grid Margin="0,0,0,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="150"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Label Grid.Column="0" Content="Description :" VerticalAlignment="Top" Margin="0,5,0,0"/>
                <TextBox x:Name="txtVehicleTypeDescription" 
                         Grid.Column="1" 
                         Height="70" 
                         Margin="5" 
                         TextWrapping="Wrap" 
                         AcceptsReturn="True"
                         VerticalScrollBarVisibility="Auto"
                         Text="{Binding SelectedVehicleType.Description, UpdateSourceTrigger=PropertyChanged}"/>
            </Grid>
        </StackPanel>

        <DataGrid x:Name="dgvVehicleTypes" 
                  Grid.Row="1" 
                  AutoGenerateColumns="True" 
                  IsReadOnly="True" 
                  SelectionMode="Single"
                  Margin="0,0,0,15"
                  SelectionChanged="DgvVehicleTypes_SelectionChanged"
                  AutoGeneratingColumn="DgvVehicleTypes_AutoGeneratingColumn"/>
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center">
            <Button x:Name="btnAdd" Content="Ajouter" Width="100" Margin="10,0" Click="BtnAdd_Click"/>
            <Button x:Name="btnUpdate" Content="Modifier" Width="100" Margin="10,0" Click="BtnUpdate_Click"/>
            <Button x:Name="btnDelete" Content="Supprimer" Width="100" Margin="10,0" Click="BtnDelete_Click"/>
            <Button x:Name="btnClear" Content="Effacer" Width="100" Margin="10,0" Click="BtnClear_Click"/>
        </StackPanel>
    </Grid>
</Window>